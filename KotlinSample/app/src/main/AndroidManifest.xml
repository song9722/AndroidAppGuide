<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.limlab.kotlinsample">

    <application
        android:name=".MainApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity> <!-- Activity의 label은 기본제목으로 사용됩니다. -->
        <activity
            android:name=".BmiJavaActivity"
            android:label="BmiJavaActivity" />
        <activity
            android:name=".BmiKotlinActivity"
            android:label="BmiKotlinActivity" />
        <activity
            android:name=".VariableJavaActivity"
            android:label="VariableJava" />
        <activity
            android:name=".VariableKotlinActivity"
            android:label="VarialbleKotlin" />

        <activity
            android:name=".ControlKotlinActivity"
            android:label="ControlKotlin"/>
        <activity
            android:name=".ControlJavaActivity"
            android:label="ControlJava"/>


    </application>

</manifest>